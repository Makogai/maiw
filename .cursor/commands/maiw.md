Execute the MAIW operation and arguments supplied after this command.

Read `core/MAIW.md` and `core/OPERATING.md`. Run `node bin/maiw.js <operation> <arguments>` from the
wrapper for clone/register/ensure/status/doctor. For learn/resume/handoff/sync/review, identify the
app and complete the corresponding agent workflow using compact shared memory. Never overwrite or
relearn valid existing memory unnecessarily.

