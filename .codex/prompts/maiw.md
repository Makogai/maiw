Read `core/MAIW.md` and execute the requested MAIW operation from the user's arguments. Use
`node bin/maiw.js` for deterministic operations and the compact memory workflow for agent work.
Preserve existing memory and report the selected app, memory status, and next action.

