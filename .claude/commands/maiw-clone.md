Use the `maiw` skill to clone and onboard an app from: `$ARGUMENTS`.
Run the clone operation, preserve existing memory, install all adapters, then continue into learning
only if memory is absent or unlearned. Ask before network access.

