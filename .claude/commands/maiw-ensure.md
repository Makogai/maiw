Use the `maiw` skill to ensure app code, memory, and every provider adapter exist: `$ARGUMENTS`.
Clone only when code is missing and a URL was supplied. Preserve all existing memory and report
whether learn or resume comes next.

